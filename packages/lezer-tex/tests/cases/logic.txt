# `if` statement

\ifx\mycmd\undefined
undefed
\fi

==>

Document(primitive,control_sequence,control_sequence,car_ret,car_ret,primitive)

# `if` `else` statement

\ifx\mycmd\undefined
undefed
\else
test
\fi

==>

Document(primitive,control_sequence,control_sequence,car_ret,car_ret,primitive,car_ret,car_ret,primitive)

# Nested `if` `else` statement 1

\ifx\mycmd\undefined
undefed
\else
\ifx\mycmd\undefined
undefed
\fi
\fi

==>

Document(primitive,control_sequence,control_sequence,car_ret,car_ret,primitive,car_ret,primitive,control_sequence,control_sequence,car_ret,car_ret,primitive,car_ret,primitive)

# Nested `if` `else` statement 2

\ifx\mycmd\undefined
  undefed
\else
  \ifx\mycmd\undefined
    undefed
  \else
    \ifx\mycmd\undefined
      undefed
    \fi
  \fi
\fi

==>

Document(primitive,control_sequence,control_sequence,car_ret,car_ret,primitive,car_ret,primitive,control_sequence,control_sequence,car_ret,car_ret,primitive,car_ret,primitive,control_sequence,control_sequence,car_ret,car_ret,primitive,car_ret,primitive,car_ret,primitive)

# Nested `if` `else` statement 3

\ifx\mycmd\undefined
undefed
\else
  \if\mycmd1
  defed, 1
  \else
  defed
  \fi
\fi

==>

Document(primitive,control_sequence,control_sequence,car_ret,car_ret,primitive,car_ret,primitive,control_sequence,car_ret,car_ret,primitive,car_ret,car_ret,primitive,car_ret,primitive)